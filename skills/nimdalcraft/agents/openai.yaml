interface:
  display_name: "Nimdalcraft"
  short_description: "Turn product ideas into feature-level OSS code retrieval and reconstruction plans"
  default_prompt: "Use $nimdalcraft to turn this idea into a code retrieval and reconstruction plan that adapts OSS patterns to the user's project."
